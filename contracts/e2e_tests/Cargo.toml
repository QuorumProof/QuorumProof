[package]
name = "e2e_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.11", features = ["json"] }
h2 = "=0.4.16"  # Pin h2 to fixed security version
dotenv = "0.15"
anyhow = "1"

[dev-dependencies]
tokio-test = "0.4"
